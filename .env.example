# Optional local override. Production uses the deployed Cloudflare Worker.
VITE_NEWSLETTER_API_URL=https://visual-explainers-newsletter.nicholasbardy.workers.dev

# Enable browser analytics in local development only when explicitly testing.
VITE_ANALYTICS_DEV=false
